#include "main.h"
/**
 * print_alphabet_x10- prints  _puchar
 */
void print_alphabet_x10(void)
{
	char le;

	int k;

	for (k = 1; k <= 10; k++)
	{
		for (le = 'a'; le <= 'z'; le++)
		{
			_putchar(le);
		}
		_putchar('\n';
	}
}
